/* glslc: warning '#pragma once' : not implemented */

#ifndef BLOCK_INC
#define BLOCK_INC

#define BLOCK_X_BITS 5
#define BLOCK_Y_BITS 5
#define BLOCK_Z_BITS 5
#define BLOCK_U_BITS 4
#define BLOCK_V_BITS 4
#define BLOCK_DIRECTION_BITS 3
#define BLOCK_LIGHT_BITS 4

#define BLOCK_X_OFFSET (0)
#define BLOCK_Y_OFFSET (BLOCK_X_OFFSET + BLOCK_X_BITS)
#define BLOCK_Z_OFFSET (BLOCK_Y_OFFSET + BLOCK_Y_BITS)
#define BLOCK_U_OFFSET (BLOCK_Z_OFFSET + BLOCK_Z_BITS)
#define BLOCK_V_OFFSET (BLOCK_U_OFFSET + BLOCK_U_BITS)
#define BLOCK_DIRECTION_OFFSET (BLOCK_V_OFFSET + BLOCK_V_BITS)
#define BLOCK_LIGHT_OFFSET (BLOCK_DIRECTION_OFFSET + BLOCK_DIRECTION_BITS)

#define BLOCK_X_MASK ((1 << BLOCK_X_BITS) - 1)
#define BLOCK_Y_MASK ((1 << BLOCK_Y_BITS) - 1)
#define BLOCK_Z_MASK ((1 << BLOCK_Z_BITS) - 1)
#define BLOCK_U_MASK ((1 << BLOCK_U_BITS) - 1)
#define BLOCK_V_MASK ((1 << BLOCK_V_BITS) - 1)
#define BLOCK_DIRECTION_MASK ((1 << BLOCK_DIRECTION_BITS) - 1)
#define BLOCK_LIGHT_MASK ((1 << BLOCK_LIGHT_BITS) - 1)

#endif